name: Deploy Blazor WASM

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Publish
        run: dotnet publish -c Release

      - name: Create 404 page
        run: |
          cp $(find . -path "*/publish/wwwroot/index.html" | head -n 1) \
             $(find . -path "*/publish/wwwroot/index.html" | head -n 1 | sed 's/index.html/404.html/')

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: $(find . -path "*/publish/wwwroot" | head -n 1)